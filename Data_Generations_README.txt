In order to grab .csv files of the data, you need to first import the already existing .csv files over the corresponding
tables (which you can find in vscode's database tab). Then, use the instructions written in comments on the .sql files.
After the query is run (and should look like a bland table), download the table and convert using microsoft excel.